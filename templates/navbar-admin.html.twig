<header class="topbar" data-navbarbg="skin5">
        <nav class="navbar top-navbar navbar-expand-md navbar-dark">
          <div class="navbar-header" data-logobg="skin5">
            <!-- ============================================================== -->
            <!-- Logo -->
            <!-- ============================================================== -->
            <a class="navbar-brand" href="{{path('app_home')}}">
              <!-- Logo icon -->
              <b class="logo-icon ps-2" style=" padding-left: 0rem!important;">
                <!--You can put here icon as well // <i class="wi wi-sunset"></i> //-->
                <!-- Dark Logo icon -->
                <img
                  src="{{  asset('theme/assets/images/img-logo-bo.png')}}"
                  alt="homepage"
                  class="light-logo"
                  style=" width: 39px;"
                />
              </b>
              <!--End Logo icon -->
              <!-- Logo text -->
              <span class="logo-text ms-2">
                 <p style="margin-bottom: 0;font-weight: 800;   font-style: italic;    font-size: 15px;">Minsitère des finances </p>
                <!-- dark Logo text -->
                {# <img style="    width: 134px;"
                  src="{{  asset('theme/assets/images/ministerefinance-ar.png')}}"
                  alt="homepage"
                  class="light-logo"
                /> #}
              </span>
          
            </a>
            <!-- ============================================================== -->
            <!-- End Logo -->
            <!-- ============================================================== -->
            <!-- ============================================================== -->
            <!-- Toggle which is visible on mobile only -->
            <!-- ============================================================== -->
            <a
              class="nav-toggler waves-effect waves-light d-block d-md-none"
              href="javascript:void(0)"
              ><i class="ti-menu ti-close"></i
            ></a>
          </div>
          <!-- ============================================================== -->
          <!-- End Logo -->
          <!-- ============================================================== -->
          <div
            class="navbar-collapse collapse"
            id="navbarSupportedContent"
            data-navbarbg="skin5"
          >
            <!-- ============================================================== -->
            <!-- toggle and nav items -->
            <!-- ============================================================== -->
            <ul class="navbar-nav float-start me-auto">
              <li class="nav-item d-none d-lg-block">
                <a
                  class="nav-link sidebartoggler waves-effect waves-light"
                  href="javascript:void(0)"
                  data-sidebartype="mini-sidebar"
                  ><i class="mdi mdi-menu font-24"></i
                ></a>
              </li>
           
            </ul>
            <!-- ============================================================== -->
            <!-- Right side toggle and nav items -->
            <!-- ============================================================== -->
            <ul class="navbar-nav float-end">
             
              <!-- ============================================================== -->
              <!-- User profile and search -->
              <!-- ============================================================== -->
              <li class="nav-item dropdown li-profile">
                <a
                  class="
                    nav-link
                    dropdown-toggle
                    text-muted
                    waves-effect waves-dark
                    pro-pic
                  "
                  href="#"
                  id="navbarDropdown"
                  role="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                >
                  <img
                    src="{{  asset('theme/assets/images/users/1.jpg')}}"
                    alt="user"
                    class="rounded-circle"
                    width="31"
                  />
                </a>
                <ul
                  class="dropdown-menu dropdown-menu-end user-dd animated"
                  aria-labelledby="navbarDropdown"
                ><a class="dropdown-item" href="javascript:void(0)"
                    ><i class="mdi mdi-account me-1 ms-1"></i> {% if is_granted("ROLE_USER") %}
                    {{ app.user.fullName|default('') }}
                    {% endif %}</a
                  >
                  <a class="dropdown-item" href="javascript:void(0)"
                    ><i class="me-2 mdi mdi-email"></i> {% if is_granted("ROLE_USER") %}
                    {{ app.user.username|default('') }}
                    {% endif %}</a
                  >
                  {# <a
                  href="{{path('user_change_password')}}"
                  class="dropdown-item"
            ><i class="me-2 mdi mdi-key-variant"></i>Changer Mot de passe </a
                > #}
                  <div class="dropdown-divider"></div>
                  <div class="ps-4 p-10">
                    {# <a
                      href="{{path('user_edit')}}"
                      class="btn btn-sm btn-success btn-rounded text-white"
                ><i class="me-2 mdi mdi-account-check"></i>Voir Profil</a
                    >  #}
                    
                    <a
                    href="{{ path('security_logout', {'logout' : csrf_token('logout')}) }}"
                    class="btn btn-sm btn-info btn-rounded text-white"
                    ><i class="fa fa-power-off me-1 ms-1"></i> Déconnexion</a
                  >
                  </div>
                </ul>
              </li>
              <!-- ============================================================== -->
              <!-- User profile and search -->
              <!-- ============================================================== -->
            </ul>
          </div>
        </nav>
        
      </header>
    <style>
      .table thead th {
            font-weight: 700 !important;
        }
    </style>